# Example environment variables file
# Copy this file to .env to enable premium features
# Note: The application works with limited features without API keys

# API Key for speech-to-text transcription (optional)
# VITE_HF_API_KEY=your_api_key_here

# The same API key is used for both transcription and text processing